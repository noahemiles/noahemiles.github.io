function divClick() {

    let project_content = document.getElementById('project-content');

    //for items in git
    let newDiv = document.createElement('div');
    newDiv.id = "project-panel";

    project_content.appendChild(newDiv);
}
